import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bird-cam-ui',
  webDir: '../../dist/apps/bird-cam-ui',
  bundledWebRuntime: false,
};

export default config;
