services:
  janus:
    build: .
    volumes:
      - /portainer/Files/AppData/Config/nginx-proxy-manager/letsencrypt:/etc/letsencrypt
      - /home/pi/janus-docker/configs:/opt/janus/etc/janus
      - /home/pi/janus-docker/recordings:/home/recordings
    tty: true
    network_mode: "host"    
    restart: always  
